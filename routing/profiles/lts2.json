{
  "distance_influence": 30,
  "priority": [
    { "if": "road_class == MOTORWAY || road_class == TRUNK", "multiply_by": 0.0 },
    { "if": "road_class != CYCLEWAY && road_class != PATH && road_class != PEDESTRIAN && bike_network == MISSING", "multiply_by": 0.2 }
  ],
  "speed": [
    { "if": "true", "multiply_by": 0.75 }
  ]
}